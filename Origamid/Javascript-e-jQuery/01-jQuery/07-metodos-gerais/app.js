$('a').attr('href');
$('a').attr('href', 'www.google.com.br');

$('a').removeAttr('href');

var name = $('input.name').val();

$('.modal').remove(); // remove  o item
$('.modal').empty(); // remove o quem tem dentro
