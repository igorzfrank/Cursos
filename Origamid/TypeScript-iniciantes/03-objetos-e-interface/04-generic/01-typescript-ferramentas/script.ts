function somar(a:number, b:number ) {
  return a + b;
}

console.log(somar(5, 5));

// npm install -g typescript                                - (instala o typescript globalmente)
// tsc --init                                               - (inicia o typescript no projeto)
// {"compilerOptions": {"target": "ESNext","strict": true}} - (compila para a versão mais nova de JS)
// tsc -w                                                   - (inicia o modo de desenvolvimento)
